/*
import type { API, Formats, Perms, Units } from 'homebridge';

// declare class
interface CustomHomeKitTypes {
  // Perms: Perms;
  // Formats: Formats;
  // Units: Units;
  hapCharacteristics: {
    StatusActive: Function;
  };
  Characteristics: {
    LastUpdated: Function;
  };

  constructor(homebridge: API);

  createCharacteristicClass(key, uuid, props, displayName = key);

  createServiceClass(key, uuid, characteristics);
}
*/
